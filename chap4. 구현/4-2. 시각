/* "완전 탐색 문제"

입력 : 정수 N
출력 : 00:00:00~0N:59:59 까지 모든 시각 중 3이 하나라도 포함되는 모든 경우의 수 출력

*/

N=int(input())

count=0
for i in rage(N+1):
  for j in range(60):
    for k in range(60):
      if '3' in str(i)+str(j)+str(k):
        count+=1

print(count)

